name: restoranarimbi
description: "A Flutter project for showcasing a restaurant profile."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  url_launcher: ^6.1.7  # Tambahkan url_launcher

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - images/nasigoreng.png
    - images/ayambakar.png
    - images/sateayam.png
    - images/gambar1.png
